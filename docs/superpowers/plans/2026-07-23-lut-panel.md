# LUT-панель: компактный режим, папки и избранное — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить в левую LUT-панель переключатель compact/expanded вида, виртуальные папки и избранные LUT, переиспользуя код и стили панели пресетов.

**Architecture:** Новый `useLutStore` хранит дерево папок, порядок LUT и избранное, загружая список LUT с диска и мержась с `AppSettings`. `LutsPanel` рендерит либо компактные строки (как пресеты), либо текущую плиточную сетку, с папками и drag-and-drop поверх. D-оверлей использует избранное с fallback на весь список.

**Tech Stack:** React + TypeScript, Zustand, @dnd-kit/core, Tauri invoke, Tailwind CSS, i18next.

---

## File map

| File | Responsibility |
|------|----------------|
| `src/components/ui/AppProperties.tsx` | Frontend types: `LutFolder`, `AppSettings` additions. |
| `src-tauri/src/app_settings.rs` | Rust `AppSettings` additions. |
| `src/store/useLutStore.ts` | LUT list, folders, order, favorites, DnD mutations, persistence. |
| `src/components/panel/left/LutsPanel.tsx` | Header toggle, compact/expanded rendering, folders, DnD, context menus. |
| `src/hooks/useGestureAdjust.ts` | D-key overlay: favorites first, then all LUTs. |
| `src/i18n/locales/en.json` | New locale keys. |

---

### Task 1: Add `LutFolder` type and settings fields to `AppProperties.tsx`

**Files:**
- Modify: `src/components/ui/AppProperties.tsx:187-269`

- [ ] **Step 1: Add `LutFolder` interface before `LutFileSettings`**

```ts
export interface LutFolder {
  id: string;
  name: string;
  children: string[];
}
```

- [ ] **Step 2: Extend `AppSettings` with three new fields**

Add inside `AppSettings` after `lutSettings?: Record<string, LutFileSettings>;`:

```ts
  lutFolders?: LutFolder[];
  lutOrder?: string[];
  lutFavorites?: string[];
```

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/AppProperties.tsx
git commit -m "add lut folder/favorite/order types to AppSettings"
```

---

### Task 2: Add Rust `AppSettings` fields

**Files:**
- Modify: `src-tauri/src/app_settings.rs:335-365`, `src-tauri/src/app_settings.rs:500-512`, `src-tauri/src/app_settings.rs:650-700`

- [ ] **Step 1: Add `LutFolder` struct after `LutFileSettings`**

```rust
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
#[serde(rename_all = "camelCase")]
pub struct LutFolder {
    pub id: String,
    pub name: String,
    pub children: Vec<String>,
}
```

- [ ] **Step 2: Add fields to `AppSettings` struct after `lut_settings`**

```rust
    #[serde(default)]
    pub lut_folders: Vec<LutFolder>,
    #[serde(default)]
    pub lut_order: Vec<String>,
    #[serde(default)]
    pub lut_favorites: Vec<String>,
```

- [ ] **Step 3: Add default values in `Default for AppSettings`**

Find the `Default` impl and add after `lut_settings: HashMap::new(),`:

```rust
            lut_folders: Vec::new(),
            lut_order: Vec::new(),
            lut_favorites: Vec::new(),
```

- [ ] **Step 4: Run cargo check**

```bash
cd src-tauri && cargo check
```
Expected: success (only pre-existing warnings).

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/app_settings.rs
git commit -m "add lut folders, order and favorites to rust AppSettings"
```

---

### Task 3: Create `useLutStore.ts` skeleton

**Files:**
- Create: `src/store/useLutStore.ts`

- [ ] **Step 1: Create the file with state, load action and persistence**

```ts
import { create } from 'zustand';
import { invoke } from '@tauri-apps/api/core';
import debounce from 'lodash.debounce';
import { LutFolder, Invokes } from '../components/ui/AppProperties';
import { useSettingsStore } from './useSettingsStore';

export interface LutEntry {
  name: string;
  path: string;
}

export enum LutListType {
  Folder = 'folder',
  Lut = 'lut',
}

export interface LutDisplayItem {
  type: LutListType.Lut;
  entry: LutEntry;
}

export interface LutFolderItem {
  type: LutListType.Folder;
  folder: LutFolder;
}

export type LutListItem = LutDisplayItem | LutFolderItem;

function arrayMove<T>(array: T[], from: number, to: number): T[] {
  const next = array.slice();
  const [item] = next.splice(from, 1);
  next.splice(to, 0, item);
  return next;
}

interface LutState {
  entries: LutEntry[];
  folders: LutFolder[];
  order: string[];
  favorites: Set<string>;
  isLoading: boolean;

  loadLuts: () => Promise<void>;
  addFolder: (name: string) => void;
  renameFolder: (id: string, name: string) => void;
  deleteFolder: (id: string, moveChildrenToRoot: boolean) => void;
  moveLutToFolder: (path: string, folderId: string | null, overPath?: string | null) => void;
  reorderLut: (activePath: string, overPath: string) => void;
  reorderFolderLut: (folderId: string, activePath: string, overPath: string) => void;
  toggleFavorite: (path: string) => void;
  setViewMode: (mode: 'compact' | 'expanded') => void;
  viewMode: 'compact' | 'expanded';
}

const saveSettings = debounce((patch: Partial<NonNullable<unknown>>) => {
  const current = useSettingsStore.getState().appSettings;
  if (!current) return;
  const next = { ...current, ...patch };
  useSettingsStore.getState().handleSettingsChange(next).catch((err) => console.error('Failed to save LUT settings:', err));
}, 500);

export const useLutStore = create<LutState>((set, get) => ({
  entries: [],
  folders: [],
  order: [],
  favorites: new Set(),
  isLoading: true,
  viewMode: 'expanded',

  loadLuts: async () => {
    set({ isLoading: true });
    try {
      const entries = await invoke<LutEntry[]>('list_luts');
      const settings = useSettingsStore.getState().appSettings;
      const rawFolders = settings?.lutFolders ?? [];
      const rawOrder = settings?.lutOrder ?? [];
      const rawFavorites = settings?.lutFavorites ?? [];

      // Clean up stale paths from folders/order/favorites.
      const existingPaths = new Set(entries.map((e) => e.path));
      const folders = rawFolders
        .map((f) => ({ ...f, children: f.children.filter((p) => existingPaths.has(p)) }))
        .filter((f) => f.children.length > 0 || rawFolders.some((rf) => rf.id === f.id));
      const order = rawOrder.filter((p) => existingPaths.has(p));
      const favorites = new Set(rawFavorites.filter((p) => existingPaths.has(p)));

      set({
        entries,
        folders,
        order,
        favorites,
        viewMode: (settings as any)?.lutViewMode ?? 'expanded',
        isLoading: false,
      });

      if (
        JSON.stringify(folders) !== JSON.stringify(rawFolders) ||
        JSON.stringify(order) !== JSON.stringify(rawOrder) ||
        JSON.stringify([...favorites]) !== JSON.stringify(rawFavorites)
      ) {
        saveSettings({ lutFolders: folders, lutOrder: order, lutFavorites: [...favorites] });
      }
    } catch (error) {
      console.error('Failed to load LUTs:', error);
      set({ isLoading: false });
    }
  },

  addFolder: (name: string) => {
    const { folders, entries, order } = get();
    const newFolder: LutFolder = { id: crypto.randomUUID(), name, children: [] };
    // Insert before first LUT at root so folders stay at the top.
    const firstLutIndex = order.length > 0 ? 0 : -1;
    let nextFolders: LutFolder[];
    if (firstLutIndex === -1) {
      nextFolders = [...folders, newFolder];
    } else {
      nextFolders = [...folders];
      nextFolders.splice(firstLutIndex, 0, newFolder);
    }
    set({ folders: nextFolders });
    saveSettings({ lutFolders: nextFolders });
  },

  renameFolder: (id: string, name: string) => {
    set((state) => {
      const folders = state.folders.map((f) => (f.id === id ? { ...f, name } : f));
      saveSettings({ lutFolders: folders });
      return { folders };
    });
  },

  deleteFolder: (id: string, moveChildrenToRoot: boolean) => {
    set((state) => {
      const folder = state.folders.find((f) => f.id === id);
      const children = folder?.children ?? [];
      const remainingFolders = state.folders.filter((f) => f.id !== id);
      let order = state.order;
      if (moveChildrenToRoot) {
        order = [...children.filter((p) => !state.order.includes(p)), ...order];
      }
      saveSettings({ lutFolders: remainingFolders, lutOrder: order });
      return { folders: remainingFolders, order };
    });
  },

  moveLutToFolder: (path: string, folderId: string | null, overPath: string | null = null) => {
    set((state) => {
      const sourceFolder = state.folders.find((f) => f.children.includes(path)) ?? null;
      let folders = state.folders;
      let order = state.order;

      // Remove from source.
      if (sourceFolder) {
        folders = folders.map((f) =>
          f.id === sourceFolder.id ? { ...f, children: f.children.filter((p) => p !== path) } : f,
        );
      } else {
        order = order.filter((p) => p !== path);
      }

      // Add to target.
      if (folderId) {
        folders = folders.map((f) => {
          if (f.id !== folderId) return f;
          const newChildren = [...f.children];
          if (overPath) {
            const overIndex = newChildren.indexOf(overPath);
            if (overIndex !== -1) newChildren.splice(overIndex, 0, path);
            else newChildren.push(path);
          } else {
            newChildren.push(path);
          }
          return { ...f, children: newChildren };
        });
      } else {
        const newOrder = [...order];
        if (overPath) {
          const overIndex = newOrder.indexOf(overPath);
          if (overIndex !== -1) newOrder.splice(overIndex, 0, path);
          else newOrder.push(path);
        } else {
          newOrder.push(path);
        }
        order = newOrder;
      }

      saveSettings({ lutFolders: folders, lutOrder: order });
      return { folders, order };
    });
  },

  reorderLut: (activePath: string, overPath: string) => {
    set((state) => {
      const from = state.order.indexOf(activePath);
      const to = state.order.indexOf(overPath);
      if (from === -1 || to === -1) return state;
      const order = arrayMove(state.order, from, to);
      saveSettings({ lutOrder: order });
      return { order };
    });
  },

  reorderFolderLut: (folderId: string, activePath: string, overPath: string) => {
    set((state) => {
      const folder = state.folders.find((f) => f.id === folderId);
      if (!folder) return state;
      const from = folder.children.indexOf(activePath);
      const to = folder.children.indexOf(overPath);
      if (from === -1 || to === -1) return state;
      const children = arrayMove(folder.children, from, to);
      const folders = state.folders.map((f) => (f.id === folderId ? { ...f, children } : f));
      saveSettings({ lutFolders: folders });
      return { folders };
    });
  },

  toggleFavorite: (path: string) => {
    set((state) => {
      const favorites = new Set(state.favorites);
      if (favorites.has(path)) favorites.delete(path);
      else favorites.add(path);
      saveSettings({ lutFavorites: [...favorites] });
      return { favorites };
    });
  },

  setViewMode: (mode: 'compact' | 'expanded') => {
    set({ viewMode: mode });
    saveSettings({ lutViewMode: mode });
  },
}));
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx tsc --noEmit -p tsconfig.json 2>&1 | head -40
```
Expected: no new errors (baseline may have existing errors).

- [ ] **Step 3: Commit**

```bash
git add src/store/useLutStore.ts
git commit -m "add useLutStore for lut folders, order and favorites"
```

---

### Task 4: Wire `useLutStore` into `LutsPanel`

**Files:**
- Modify: `src/components/panel/left/LutsPanel.tsx:49-147`

- [ ] **Step 1: Replace local `entries` state with store**

Remove:

```ts
  const [entries, setEntries] = useState<LutEntry[]>([]);
```

Add near other hooks:

```ts
  const { entries, folders, order, loadLuts, isLoading: isLoadingEntries } = useLutStore();
```

- [ ] **Step 2: Replace `refreshList` with store load and update import handlers**

Remove `refreshList` callback and its `useEffect`. Add:

```ts
  useEffect(() => {
    loadLuts();
  }, [loadLuts]);
```

In `handleImport`, replace `setEntries(list);` with `loadLuts();`.

In `onSaved` of `ConfigureLutHotkeyModal`, replace `refreshList();` with `loadLuts();`.

In delete confirmation, replace `refreshList();` with `loadLuts();`.

- [ ] **Step 3: Build ordered root list for rendering**

Before rendering, compute:

```ts
  const orderedEntries = useMemo(() => {
    const map = new Map(entries.map((e) => [e.path, e]));
    const ordered = order.map((path) => map.get(path)).filter(Boolean) as LutEntry[];
    const remaining = entries.filter((e) => !order.includes(e.path));
    return [...ordered, ...remaining];
  }, [entries, order]);
```

Replace usages of `entries` in the render loop with `orderedEntries`.

- [ ] **Step 4: Commit**

```bash
git add src/components/panel/left/LutsPanel.tsx
git commit -m "wire useLutStore into LutsPanel"
```

---

### Task 5: Add compact/expanded view toggle to `LutsPanel` header

**Files:**
- Modify: `src/components/panel/left/LutsPanel.tsx:1-50`, `src/components/panel/left/LutsPanel.tsx:455-467`

- [ ] **Step 1: Import icons and store selector**

Add to imports:

```ts
import { LayoutGrid, List } from 'lucide-react';
import { useLutStore } from '../../../store/useLutStore';
```

- [ ] **Step 2: Read view mode from store**

```ts
  const { viewMode, setViewMode } = useLutStore();
```

- [ ] **Step 3: Add toggle button next to import button in header**

Inside the header flex after the import button:

```tsx
        <button
          className="p-2 rounded-full hover:bg-surface transition-colors"
          onClick={() => setViewMode(viewMode === 'expanded' ? 'compact' : 'expanded')}
          data-tooltip={viewMode === 'expanded' ? t('ui.lut.compactView') : t('ui.lut.expandedView')}
          aria-label={viewMode === 'expanded' ? t('ui.lut.compactView') : t('ui.lut.expandedView')}
        >
          {viewMode === 'expanded' ? <List size={18} /> : <LayoutGrid size={18} />}
        </button>
```

- [ ] **Step 4: Commit**

```bash
git add src/components/panel/left/LutsPanel.tsx
git commit -m "add compact/expanded view toggle to LutsPanel header"
```

---

### Task 6: Implement compact LUT row component

**Files:**
- Modify: `src/components/panel/left/LutsPanel.tsx:505-603`

- [ ] **Step 1: Create `CompactLutRow` component inside `LutsPanel.tsx`**

Before `LutDetailPanel`:

```tsx
interface CompactLutRowProps {
  entry: LutEntry;
  thumb: string | null;
  isSelected: boolean;
  isLoading: boolean;
  hotkey: string[] | null;
  osPlatform: string;
  onSelect: (path: string) => void;
  onContextMenu: (event: React.MouseEvent, entry: LutEntry) => void;
  onMouseEnter: () => void;
  onMouseLeave: () => void;
}

function CompactLutRow({
  entry,
  thumb,
  isSelected,
  isLoading,
  hotkey,
  osPlatform,
  onSelect,
  onContextMenu,
  onMouseEnter,
  onMouseLeave,
}: CompactLutRowProps) {
  return (
    <div
      className={clsx(
        'flex items-center gap-3 p-2 rounded-lg bg-surface cursor-pointer outline-2 outline-offset-[-2px] transition-[outline-color]',
        isSelected ? 'outline-accent' : 'outline-transparent hover:outline-accent/50',
      )}
      onClick={() => onSelect(entry.path)}
      onContextMenu={(e) => onContextMenu(e, entry)}
      onMouseEnter={onMouseEnter}
      onMouseLeave={onMouseLeave}
    >
      <div className="w-20 h-14 bg-bg-tertiary rounded-md flex items-center justify-center shrink-0 relative overflow-hidden">
        {isLoading && thumb === undefined ? (
          <Loader2 size={20} className="animate-spin text-text-secondary" />
        ) : thumb ? (
          <img src={thumb} alt={entry.name} className="w-full h-full object-cover rounded-md pointer-events-none" />
        ) : (
          <ImageOff size={18} className="text-text-secondary" />
        )}
      </div>
      <div className="grow min-w-0 flex flex-col justify-center">
        <div className="flex items-center gap-2">
          <Text color={TextColors.primary} weight={isSelected ? TextWeights.medium : TextWeights.normal} className="truncate">
            {entry.name}
          </Text>
          {hotkey && hotkey.length > 0 && (
            <Text
              as="kbd"
              variant={TextVariants.small}
              color={TextColors.secondary}
              className="px-1.5 py-0.5 bg-bg-primary border border-border-color rounded text-[10px] shrink-0"
            >
              {hotkey.map((k) => formatKeyCode(k, osPlatform)).join('')}
            </Text>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Render compact list when `viewMode === 'compact'`**

Replace the current grid render block with a conditional. Keep grid for `expanded`. For `compact`, render a vertical stack of `CompactLutRow` components, and below the selected row render `LutDetailPanel`.

```tsx
        {viewMode === 'compact' ? (
          <div className="space-y-2">
            {orderedEntries.map((entry) => {
              const isSelected = entry.path === selectedLutPath;
              return (
                <div key={entry.path} className="flex flex-col">
                  <CompactLutRow
                    entry={entry}
                    thumb={previews[entry.path] ?? null}
                    isSelected={isSelected}
                    isLoading={isLoadingPreviews}
                    hotkey={appSettings?.lutSettings?.[entry.path]?.hotkey ?? null}
                    osPlatform={osPlatform}
                    onSelect={handleSelect}
                    onContextMenu={handleContextMenu}
                    onMouseEnter={() => setLutPreviewOverride(entry.path)}
                    onMouseLeave={() => setLutPreviewOverride(null)}
                  />
                  <AnimatePresence initial={false}>
                    {isSelected && (
                      <motion.div
                        initial={{ height: 0, opacity: 0 }}
                        animate={{ height: 'auto', opacity: 1 }}
                        exit={{ height: 0, opacity: 0 }}
                        transition={{ duration: 0.25, ease: 'easeInOut' }}
                        className="w-full cursor-auto overflow-hidden"
                        onClick={(e) => e.stopPropagation()}
                        onPointerDown={(e) => e.stopPropagation()}
                      >
                        <div className="mt-3 px-2 pb-2">
                          <LutDetailPanel ... />
                        </div>
                      </motion.div>
                    )}
                  </AnimatePresence>
                </div>
              );
            })}
          </div>
        ) : (
          // existing expanded grid render
        )}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/panel/left/LutsPanel.tsx
git commit -m "add compact LUT row rendering"
```

---

### Task 7: Render folder headers

**Files:**
- Modify: `src/components/panel/left/LutsPanel.tsx`

- [ ] **Step 1: Add folder icon imports**

```ts
import { Folder as FolderIcon, FolderOpen } from 'lucide-react';
```

- [ ] **Step 2: Add local expanded folder state**

```ts
  const [expandedFolders, setExpandedFolders] = useState(new Set<string>());
```

- [ ] **Step 3: Create `FolderHeader` component**

```tsx
interface FolderHeaderProps {
  folder: LutFolder;
  isExpanded: boolean;
  onToggle: (id: string) => void;
  onContextMenu: (event: React.MouseEvent, folder: LutFolder) => void;
}

function FolderHeader({ folder, isExpanded, onToggle, onContextMenu }: FolderHeaderProps) {
  return (
    <div
      className="flex items-center gap-2 p-2 rounded-lg bg-surface cursor-pointer"
      onContextMenu={(e) => onContextMenu(e, folder)}
    >
      <div className="p-1" onClick={(e) => { e.stopPropagation(); onToggle(folder.id); }}>
        {isExpanded ? <FolderOpen className="text-primary" size={18} /> : <FolderIcon className="text-text-secondary" size={18} />}
      </div>
      <Text color={TextColors.primary} weight={TextWeights.medium} className="grow truncate select-none" onClick={() => onToggle(folder.id)}>
        {folder.name}
      </Text>
      <Text as="span" variant={TextVariants.small} color={TextColors.secondary} className="ml-auto pr-1">
        {folder.children.length}
      </Text>
    </div>
  );
}
```

- [ ] **Step 4: Build mixed item list and render folders + LUTs**

Create helper:

```ts
  const mixedItems = useMemo<LutListItem[]>(() => {
    const rootPaths = new Set(orderedEntries.map((e) => e.path));
    folders.forEach((f) => f.children.forEach((p) => rootPaths.delete(p)));
    const rootEntries = orderedEntries.filter((e) => rootPaths.has(e.path));

    const items: LutListItem[] = [];
    folders.forEach((folder) => items.push({ type: LutListType.Folder, folder }));
    rootEntries.forEach((entry) => items.push({ type: LutListType.Lut, entry }));
    return items;
  }, [folders, orderedEntries]);
```

Render in compact mode:

```tsx
            {mixedItems.map((item) => {
              if (item.type === LutListType.Folder) {
                const folder = item.folder;
                const isExpanded = expandedFolders.has(folder.id);
                return (
                  <div key={folder.id}>
                    <FolderHeader folder={folder} isExpanded={isExpanded} onToggle={toggleFolder} onContextMenu={handleFolderContextMenu} />
                    <AnimatePresence>
                      {isExpanded && (
                        <motion.div
                          animate={{ height: 'auto', opacity: 1 }}
                          className="ml-5 pl-4 border-l-[1.5px] border-border-color/50 space-y-2 pt-2"
                          exit={{ height: 0, opacity: 0 }}
                          initial={{ height: 0, opacity: 0 }}
                        >
                          {folder.children
                            .map((path) => orderedEntries.find((e) => e.path === path))
                            .filter(Boolean)
                            .map((entry) => (
                              <CompactLutRow key={entry!.path} ... />
                            ))}
                        </motion.div>
                      )}
                    </AnimatePresence>
                  </div>
                );
              }
              // render CompactLutRow
            })}
```

- [ ] **Step 5: Commit**

```bash
git add src/components/panel/left/LutsPanel.tsx
git commit -m "render virtual folders in compact LUT list"
```

---

### Task 8: Add drag-and-drop for LUT items and folders

**Files:**
- Modify: `src/components/panel/left/LutsPanel.tsx`, `src/store/useLutStore.ts`

- [ ] **Step 1: Add @dnd-kit imports**

```ts
import { DndContext, DragOverlay, PointerSensor, useDraggable, useDroppable, useSensor, useSensors } from '@dnd-kit/core';
```

- [ ] **Step 2: Create `DraggableLutRow` wrapper**

```tsx
interface DraggableLutRowProps extends CompactLutRowProps {
  folderId?: string | null;
}

function DraggableLutRow({ folderId, ...props }: DraggableLutRowProps) {
  const { attributes, listeners, setNodeRef: setDragRef, isDragging } = useDraggable({
    id: props.entry.path,
    data: { type: LutListType.Lut, path: props.entry.path, folderId },
  });
  const { setNodeRef: setDropRef, isOver } = useDroppable({
    id: props.entry.path,
    data: { type: LutListType.Lut, path: props.entry.path, folderId },
  });
  const setRef = useCallback(
    (node: HTMLElement | null) => {
      setDragRef(node);
      setDropRef(node);
    },
    [setDragRef, setDropRef],
  );
  return (
    <div ref={setRef} style={{ opacity: isDragging ? 0.4 : 1, outline: isOver ? '2px solid var(--color-primary)' : '2px solid transparent', outlineOffset: '-2px' }} {...listeners} {...attributes}>
      <CompactLutRow {...props} />
    </div>
  );
}
```

- [ ] **Step 3: Create `DraggableFolderHeader`**

Similar wrapper for folders.

- [ ] **Step 4: Wire `DndContext` around the list and implement `onDragEnd`**

```tsx
  const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 10 } }));

  const handleDragEnd = (event: any) => {
    const { active, over } = event;
    if (!over || active.id === over.id) return;
    const activeType = active.data.current?.type;
    const overType = over.data.current?.type;
    const activePath = active.data.current?.path;
    const activeFolderId = active.data.current?.folderId;

    if (activeType === LutListType.Lut) {
      const targetFolderId = overType === LutListType.Folder ? over.id : over.data.current?.folderId ?? null;
      if (targetFolderId && targetFolderId !== activeFolderId) {
        moveLutToFolder(activePath, targetFolderId);
      } else if (activeFolderId) {
        reorderFolderLut(activeFolderId, activePath, over.id);
      } else {
        reorderLut(activePath, over.id);
      }
    }
  };
```

- [ ] **Step 5: Commit**

```bash
git add src/components/panel/left/LutsPanel.tsx src/store/useLutStore.ts
git commit -m "add drag-and-drop for lut folders and items"
```

---

### Task 9: Add context menus for LUT, folder and panel background

**Files:**
- Modify: `src/components/panel/left/LutsPanel.tsx:312-362`

- [ ] **Step 1: Extend `handleContextMenu` for LUT with folder/favorite items**

Add after Configure LUT item:

```ts
        {
          icon: Star,
          label: favorites.has(entry.path) ? t('ui.lut.removeFavorite') : t('ui.lut.addFavorite'),
          onClick: () => toggleFavorite(entry.path),
        },
        {
          icon: FolderPlus,
          label: t('ui.lut.addToFolder'),
          submenu: [
            ...folders.map((f) => ({ label: f.name, onClick: () => moveLutToFolder(entry.path, f.id) })),
            { label: t('ui.lut.newFolder'), icon: Plus, onClick: () => setIsAddFolderModalOpen(true) },
          ],
        },
```

- [ ] **Step 2: Add folder context menu handler**

```ts
  const handleFolderContextMenu = useCallback(
    (event: React.MouseEvent, folder: LutFolder) => {
      event.preventDefault();
      event.stopPropagation();
      showContextMenu(event.clientX, event.clientY, [
        { icon: Pencil, label: t('ui.lut.renameFolder'), onClick: () => setRenameFolderState({ isOpen: true, folder }) },
        {
          icon: Trash2,
          label: t('ui.lut.deleteFolder'),
          submenu: [
            { label: t('contextMenus.editor.cancel'), icon: X, onClick: () => {} },
            { label: t('ui.lut.deleteFolderKeepChildren'), icon: Check, onClick: () => deleteFolder(folder.id, true) },
            { label: t('ui.lut.deleteFolderRemoveChildren'), icon: Trash2, isDestructive: true, onClick: () => deleteFolder(folder.id, false) },
          ],
        },
      ]);
    },
    [showContextMenu, t, folders, deleteFolder],
  );
```

- [ ] **Step 3: Add panel background context menu**

Wrap the scrollable area with `onContextMenu`:

```tsx
      <div
        className="grow min-h-0 overflow-y-auto p-4"
        onContextMenu={(e) => {
          if (e.target === e.currentTarget) {
            e.preventDefault();
            showContextMenu(e.clientX, e.clientY, [
              { icon: FolderPlus, label: t('ui.lut.createFolder'), onClick: () => setIsAddFolderModalOpen(true) },
              { icon: viewMode === 'expanded' ? List : LayoutGrid, label: t(viewMode === 'expanded' ? 'ui.lut.compactView' : 'ui.lut.expandedView'), onClick: () => setViewMode(viewMode === 'expanded' ? 'compact' : 'expanded') },
            ]);
          }
        }}
      >
```

- [ ] **Step 4: Commit**

```bash
git add src/components/panel/left/LutsPanel.tsx
git commit -m "add lut folder and background context menus"
```

---

### Task 10: Integrate favorites into D-key LUT overlay

**Files:**
- Modify: `src/hooks/useGestureAdjust.ts:77-138`

- [ ] **Step 1: Read favorites before building strip**

```ts
    const loadLutStrip = async () => {
      try {
        const entries = await invoke<Array<{ name: string; path: string }>>('list_luts');
        if (entries.length === 0) return;

        const adjustments = useEditorStore.getState().adjustments;
        const appSettings = useSettingsStore.getState().appSettings;
        const favorites = new Set(appSettings?.lutFavorites ?? []);
        const sourceEntries = favorites.size > 0 ? entries.filter((e) => favorites.has(e.path)) : entries;

        const currentPath = adjustments.lutPath ?? null;
        const noLutEntry = { path: NO_LUT_PATH, name: t('ui.lut.disabledLut'), thumb: null };
        const stripEntries = [noLutEntry, ...sourceEntries.map((e) => ({ path: e.path, name: e.name, thumb: null }))];
        const selectedIndex = currentPath ? Math.max(1, 1 + sourceEntries.findIndex((e) => e.path === currentPath)) : 0;
```

Replace `entries` with `sourceEntries` in the rest of the function.

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useGestureAdjust.ts
git commit -m "use lut favorites in D-key overlay with all-luts fallback"
```

---

### Task 11: Add locale keys

**Files:**
- Modify: `src/i18n/locales/en.json`

- [ ] **Step 1: Add keys under `ui.lut`**

```json
    "compactView": "Compact view",
    "expandedView": "Expanded view",
    "addFavorite": "Add to favorites",
    "removeFavorite": "Remove from favorites",
    "addToFolder": "Add to folder",
    "newFolder": "New folder",
    "createFolder": "Create folder",
    "renameFolder": "Rename folder",
    "deleteFolder": "Delete folder",
    "deleteFolderKeepChildren": "Delete and move LUTs to root",
    "deleteFolderRemoveChildren": "Delete and remove LUTs",
    "removeFromFolder": "Remove from folder"
```

- [ ] **Step 2: Commit**

```bash
git add src/i18n/locales/en.json
git commit -m "add locale keys for lut folders and favorites"
```

---

### Task 12: Verify build and cargo check

- [ ] **Step 1: Frontend build**

```bash
npm run build
```
Expected: success (chunks larger than 500 kB warning is acceptable).

- [ ] **Step 2: Rust check**

```bash
cd src-tauri && cargo check
```
Expected: success (only pre-existing warnings).

- [ ] **Step 3: Final commit if any fixes were needed**

```bash
git add -A && git commit -m "fix lut panel build issues" || true
```

---

## Self-review checklist

1. **Spec coverage:** compact/expanded toggle ✓, virtual folders ✓, drag-and-drop ✓, favorites ✓, D-key overlay ✓, reuse of preset modals/styles ✓.
2. **Placeholder scan:** no TBD/TODO; all code fragments present.
3. **Type consistency:** `LutFolder` used consistently; `lutFolders`/`lutOrder`/`lutFavorites` names match in Rust and TS.
4. **Edge cases:** missing files removed from persisted lists on load ✓; deleted folder offers keep/remove children ✓; favorites fallback to all LUTs ✓.
